# 安全政策

## 支持的版本

当前项目仍处于内测和快速迭代阶段。安全修复优先支持：

| 版本 | 支持状态 |
| --- | --- |
| 当前主分支 | 支持 |
| 最近一个正式发布版本 | 尽力支持 |
| 更早版本 | 不承诺安全修复 |

如果你在旧版本中发现安全问题，请先尝试确认当前主分支是否仍然受影响。

## 哪些问题属于安全漏洞

请将以下问题视为安全漏洞：

- 登录绕过、认证失效或会话固定；
- 用户 A 可以访问、修改或删除用户 B 的数据；
- 管理员接口越权访问；
- JWT Secret、API Key、数据库密码或 Token 泄露；
- SQL 注入；
- XSS、CSRF 或开放重定向；
- 任意文件读取、写入、上传或下载；
- Excel 导入导致代码执行、路径穿越或资源耗尽；
- AI 接口被绕过限额、恶意刷量或泄露 Prompt / Key；
- 可导致服务严重中断的拒绝服务问题；
- 生产日志、备份、上传文件中泄露敏感数据。

以下通常不按安全漏洞处理，但仍欢迎通过普通 Issue 反馈：

- UI 显示问题；
- 普通业务 Bug；
- 文档错误；
- 无法复现的性能波动；
- 只影响本地开发环境且不涉及敏感数据的问题。

## 私密报告方式

请优先使用仓库平台提供的私密安全报告功能，例如 GitHub Security Advisories / Private vulnerability reporting。

当前仓库尚未提供公开安全邮箱。不要在没有维护者明确提供邮箱的情况下，把漏洞细节发送到随机邮箱或公开 Issue。

报告时请包含：

- 漏洞类型；
- 受影响版本或提交；
- 复现步骤；
- 影响范围；
- 已脱敏的请求、响应或日志；
- 你建议的修复方向，可选。

请不要包含：

- 真实用户数据；
- 真实 Token、密码、API Key；
- 可直接攻击线上服务的大规模利用脚本；
- 未经授权获取的数据样本。

## 处理流程

维护者会尽力按以下流程处理：

1. 确认报告已收到；
2. 复现并评估影响；
3. 制定修复方案；
4. 发布修复；
5. 在合适时机公开安全说明。

在修复发布前，请避免公开完整利用细节。

## 免责声明

GaitLogic Planner 是训练计划管理和日志工具，不是医疗诊断系统。安全报告应聚焦软件、数据、权限和服务稳定性问题。训练建议本身的争议请通过普通 Issue 或 Pull Request 讨论。
