# v0.16.0 Overview

v0.16 delivers retrieval alternatives without changing the training-rule boundary: dense vector retrieval, BM25, fixed Hybrid RRF, optional external reranking, safe fallback, versioned indexes and a frozen Holdout evaluation. It does not add query rewriting, a reranker-only default, new model memory, or training-plan writes.
