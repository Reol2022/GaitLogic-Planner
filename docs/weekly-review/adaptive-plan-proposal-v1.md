# Adaptive Plan Proposal v1

Phase C 只生成可审查的计划差异，不写 `PlannedWorkout`。Proposal 同时保存原值、候选值、
动作、原因和规则证据；状态从 `PENDING_APPROVAL` 开始。候选必须引用当前用户目标周中的
未锁定、未完成计划，并复用项目已有的周增量、高强度间隔和强度等级边界。

数据不足或冲突时禁止确定性调整；恢复关注或高疲劳时禁止增加跑量和强度。模型可以在规则
允许范围内组织候选内容，但不能调用数据库写操作，也不能绕过后续人工确认。
