# AGENTS

## 当前项目范围

当前项目只做训练计划和训练日志相关能力。后端 v0.1 可以实现 FastAPI 接口，但不要扩大需求范围。

## 核心数据来源

数据库设计以最新版 Excel《严飞_夏训计划与训练日志_下拉样式版.xlsx》的四个核心 Sheet 为准：

- `计划索引`
- `训练日志`
- `每周复盘`
- `配速与规则`

`总览Dashboard` 和月度计划页暂时视为展示页，不作为数据库主来源。

## 明确不要实现

- 不要实现 Garmin
- 不要实现 AI 教练
- 不要实现 App
- 不要实现小程序
- 不要实现多用户 SaaS
- 不要实现社交功能
- 不要实现前端业务
- 不要实现 Excel 解析

## 数据库扩展方向

数据库必须优先兼容后续 Excel 导入、网页制定计划、训练日志填写、统计复盘和设备同步扩展，但当前阶段只保留数据结构和扩展字段。

## 技术约束

- 使用 MySQL 8.0+
- 使用 SQLAlchemy 2.x ORM
- 使用 PyMySQL
- 使用 Pydantic 2.x / pydantic-settings
- 可以实现 FastAPI 后端接口
- 不要使用 SQLite
- 不要引入 Alembic
- 不要实现登录鉴权
